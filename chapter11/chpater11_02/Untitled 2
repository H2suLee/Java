 Chapter 11_기본 API 클래스
 
 >> Wrapper(포장) 클래스
 	- 기본 타입의 값을 갖는 객체, 기본 타입의 값을 내부에 두고 포장
 	- 포장하고 있는 기본 타입 값은 외부에ㅓㅅ 변경할 수 없음. 변경하고 싶으면 새로운 포장 객체 만들어야
 	>> Boxing 과 Unboxing
 		- Boxing: 기본 타입의 값을 포장 객체로 만드는 과정 
 			ex) (생성자 이용) Integer obj = new Integer(1000), Double obj = new Double("3.5");
 			ex) (valueOf 이용) Integer obj = Integer.valueOf("100");
 		- Unboxing: 포장 객체에서 기본 타입의 값을 얻어내는 과정
 			ex) int num = obj.IntValue();
 	>> 자동 박싱과 언박싱
 		- 포장 클래스 타입에 기본값이 대입될 경우 ex) Integer obj = 100;
 		- Interger 객체와 int 값을 연산하는 경우 ex) int value = obj + 100;	
 	>> 문자열을 기본 타입으로 변환 Integer.parseInt("") 정적 메소드
 	>> 포장값 비교 equals로
 	
 >> Math 클래스
 	- 모두 정적 메소드
 	>> int abs(int a), double abs(double a)
 	>> double ceil(double a) 올림값
 	>> double floor(double a) 내림값
 	>> int max(int a, int b) 둘 중 더 큰 값
 	>> int min(int a, int b) 둘 중 더 작은 값
 	>> double random() 0.0 <= x <1 사이 랜덤 값
 	>> double rint(doubla a) 가까운 정수의 실수값
 	>> long round(double a) 반올림값
 			